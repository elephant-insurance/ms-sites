package routes

const (
	pathGetDocument      string = `/:project/:document`
	routeNameGetDocument string = `get document`
	pathGetIndex         string = `/:project`
	routeNameGetIndex    string = `get index`
)
